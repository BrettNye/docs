---
title: 'Get Permission Overrides'
api: 'GET /scope-overrides/permissions/{childScopeId}'
description: 'Get permission overrides for a specific scope'
---

## Path Parameters

<ParamField path="childScopeId" type="string" required>
  The child scope ID to get permission overrides for
</ParamField>

## Response

Returns an array of permission override objects.

<ResponseField name="overrides" type="array">
  <Expandable title="Permission Override object">
    <ResponseField name="id" type="string">
      Unique identifier for the override
    </ResponseField>
    <ResponseField name="childScopeId" type="string">
      The scope where the override applies
    </ResponseField>
    <ResponseField name="permissionId" type="string">
      The permission being overridden
    </ResponseField>
    <ResponseField name="state" type="string">
      Override state: `enabled` or `disabled`
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET 'https://api.example.com/scope-overrides/permissions/scope_team' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```

```javascript Node.js
const axios = require('axios');

const { data } = await axios.get('https://api.example.com/scope-overrides/permissions/scope_team', {
  headers: { 'Authorization': 'Bearer YOUR_TOKEN' },
});
```
</RequestExample>

<ResponseExample>
```json Response
[
  {
    "id": "override_456",
    "childScopeId": "scope_team",
    "permissionId": "perm_delete",
    "state": "disabled"
  }
]
```
</ResponseExample>
