---
title: 'Delete Role Override'
api: 'DELETE /scope-overrides/roles/{id}'
description: 'Delete a role override by its ID'
---

## Path Parameters

<ParamField path="id" type="string" required>
  The override ID to delete
</ParamField>

## Response

Returns a success confirmation.

<RequestExample>
```bash cURL
curl -X DELETE 'https://api.example.com/scope-overrides/roles/override_123' \
  -H 'Authorization: Bearer YOUR_TOKEN'
```

```javascript Node.js
const axios = require('axios');

const { data } = await axios.delete('https://api.example.com/scope-overrides/roles/override_123', {
  headers: { 'Authorization': 'Bearer YOUR_TOKEN' },
});
```
</RequestExample>

<ResponseExample>
```json Response
{
  "success": true
}
```
</ResponseExample>
